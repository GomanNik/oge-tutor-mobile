/*
 * OGE Tutor App — mock backend public entry.
 * The implementation is isolated under src/api/mock so production adapter imports stay explicit.
 */
export { createMockBackend } from './mock/createMockBackend.js';
